<style lang="scss">
$color-theme: 200;

@function white($l:100, $a:1){
    @return hsl($color-theme,0, $l / $a); 
}

@function black($l:0, $a:1){
    @return hsl($color-theme,0 ,$l / $a); 
}

@function ct($l:50, $a:1){
    @return hsl($color-theme,100, $l / $a); 
}

:root {
    --primary: #{ct()};
    --primary-light: #{ct(80)};
    --menu-item-bg: #{ct(97)};
    --menu-item-bg-active: #{ct(90)};
    --background-color: hsl(#{$color-theme+20}, 40%,94%);
    --card-background-color: #{white()};
    --font-color: #{black()};
}

:root.dark{

}

html {
    font-family: system-ui, sans-serif;
    background: var(--background-color);
    color: var(--font-color);
}

a{
    color: var(--primary);
    text-decoration: none;
    &:hover{
        text-decoration: underline;
    }
}

code {
    font-family:
        Menlo,
        Monaco,
        Lucida Console,
        Liberation Mono,
        DejaVu Sans Mono,
        Bitstream Vera Sans Mono,
        Courier New,
        monospace;
}

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components{
    .link-default{
        color: var(--primary);
        text-decoration: none;
        &:hover{
            text-decoration: underline;
        }
    }
}
</style>